name: finance_dwh
version: '1.0.0'
config-version: 2

profile: finance_dwh

model-paths: ["models"]
seed-paths:  ["seeds"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  finance_dwh:
    staging:
      +materialized: view
    marts:
      +materialized: table
